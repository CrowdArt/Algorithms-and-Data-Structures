function reverse(str) {
    let reversed = '';

    for (lect character of str) {
        reversed = character + reversed;
    }

    return reversed;
}

module.exports = reverse;

/**
 * let is variable declaration
 * creatring temporary variable = character and is redeclared every single time through this loop
 * of iterable object = str 
 * Iterate through each character of string one by one and set each character equal to temporary 
 * variable 'character'.
 */